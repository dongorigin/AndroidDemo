<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.dong.demo" >

    <!-- 访问网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 访问储存卡 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 读取联系人 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!--  -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/AppTheme.Main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.FlowLayoutActivity" />
        <activity android:name=".ui.measure.MeasureActivity" />
        <activity android:name=".ui.anim.AnimationActivity" />
        <activity android:name=".ui.SwipeRefreshLayoutActivity" />
        <activity android:name=".ui.fragment.FragmentTabHostActivity" />
        <activity android:name=".ui.location.GeocoderActivity" />
        <activity android:name=".ui.calendar.CalendarActivity" />
        <activity android:name=".ui.IntentsActivity" />
        <activity android:name=".ui.PackageManagerActivity" />
        <activity android:name=".ui.DialogActivity" />
        <activity android:name=".ui.AutoCompleteActivity" />
        <activity android:name=".ui.ViewPagerActivity" />
        <activity android:name=".ui.TextSizeActivity" />
        <activity android:name=".ui.ContentProviderActivity" />
        <activity android:name=".ui.ShareActivity" >
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AnimActivity" />
        <activity android:name=".ui.DrawerActivity" />
        <activity android:name=".ui.PopupWindowActivity" />
        <activity android:name=".ui.touch.TouchEventActivity" />
        <activity android:name=".ui.WebViewActivity" />
        <activity android:name=".ui.fragment.FragmentTestActivity" />

        <!-- Image -->
        <activity android:name=".ui.image.DrawableStateActivity" />
        <activity android:name=".ui.image.ImageLoaderActivity" />
        <activity android:name=".ui.image.ImageActivity" />

        <!-- List -->
        <activity android:name=".ui.list.XListViewActivity" />
        <activity android:name=".ui.list.ListViewActivity" />
        <activity android:name=".ui.list.RecyclerViewActivity" />

        <provider
            android:name=".provider.MyProvider"
            android:authorities="cn.dong.demo.provider"
            android:exported="false" />

    </application>

</manifest>
